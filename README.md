Given a list of integers, use a vector and return the mean (average), median (when sorted, the value in the middle position), and mode

From: https://doc.rust-lang.org/book/second-edition/ch08-03-hash-maps.html
